{% macro render() %}
    <a href="{{ path('home') }}" class="header__main-logo">
        <img
            src="{{ asset('img/misc/logo/logo.webp') }}"
            alt="Logo"
            id="logo"
            class="header__main-logo-image"
        >
    </a>
{% endmacro %}
