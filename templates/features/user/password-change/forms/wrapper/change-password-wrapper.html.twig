{% macro render(errors = []) %}
    {% import 'components/form/action.html.twig' as FormControlAction %}
    {% import 'features/user/password-change/forms/sections/fields.html.twig' as ChangePasswordSection %}

    {{ ChangePasswordSection.render(errors ?? []) }}
    {{
        FormControlAction.render(
            'user__form-action',
            'user__form-btn',
            'Update'
        )
    }}
{% endmacro %}
