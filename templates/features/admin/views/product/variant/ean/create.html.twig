{% extends 'app/base.html.twig' %}

{% block title %}Admin - Variant EAN (Create){% endblock %}

{% block admin_body %}
    {% import 'features/admin/common/forms/form-content.html.twig' as FormContent %}

    {{
        FormContent.render(
            'admin-variant-eans-create-page',
            'Create EAN (' ~ variant.name ~ ')',
            'features/admin/views/product/variant/ean/forms/create.html.twig',
            path('admin_variants_eans_index', {'id': variant.id}),
            {
                'variant': variant,
                'errors': errors ?? []
            }
        )
    }}
{% endblock %}
