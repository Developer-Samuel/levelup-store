{% macro render(errors = []) %}
    {% import 'components/form/action.html.twig' as FormControlAction %}
    {% import 'components/form/switch.html.twig' as FormSwitch %}
    {% import 'features/auth/password/reset/forms/sections/fields.html.twig' as ResetPasswordSectionData %}

    {{ ResetPasswordSectionData.render(errors ?? []) }}

    {{
        FormControlAction.render(
            'auth-page__card-form-action',
            'auth-page__card-form-action-btn',
            'Reset Password'
        )
    }}

    {{
        FormSwitch.render(
            'Do you want to',
            path('login'),
            'Log in?'
        )
    }}
{% endmacro %}
