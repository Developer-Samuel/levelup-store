{% macro render() %}
    {% import 'features/order/create/note/list/snippets/item.html.twig' as OrderNoteItem %}

    <div class="order__card-note-list">
        {{
            OrderNoteItem.render(
                'Card number:',
                '4242 4242 4242 4242'
            )
        }}
        {{
            OrderNoteItem.render(
                'Expiration date',
                'Any future MM/YY'
            )
        }}
        {{
            OrderNoteItem.render(
                'CVC:',
                'Any 3 or 4 digit number'
            )
        }}
    </div>
{% endmacro %}
