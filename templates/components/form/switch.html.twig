{% macro render(text, link, linkText) %}
    <div class="auth-page__card-form-switch">
        {{ text }}
        <a href="{{ link }}">
            {{ linkText }}
        </a>
    </div>
{% endmacro %}
